qiskit>=2.4.1
qiskit-aer>=0.17.2
qiskit-ibm-runtime>=0.47.0
numpy>=1.24.0
scipy>=1.11.0
networkx>=3.0
pandas>=2.0.0
matplotlib>=3.7.0
python-dotenv>=1.0.0
pylatexenc
ibm-cloud-sdk-core==3.24.1
ibm-platform-services==0.66.1
pydantic-settings
requests>=2.32.4
dotenv
statsmodels

# Optional heavy dependencies are declared as extras in setup.py (not installed by default):
#   - AWS Braket support (qbraid[braket], qiskit-braket-provider) -> pip install "qiskit-qward[aws]"
#   - Torch-based metrics (torch)                                 -> pip install "qiskit-qward[torch]"
#   - Everything                                                  -> pip install "qiskit-qward[all]"
# The core library imports these lazily, so they are only required for AWS/torch features.
